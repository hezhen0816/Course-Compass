import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hezhen.courseplanner',
  appName: 'Course Planner',
  webDir: 'dist'
};

export default config;
